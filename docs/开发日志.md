# ClassBoard 开发日志与对话记录

> 本文件记录 ClassBoard 项目从立项至今的全部对话与开发过程，供跨天续接、回溯决策。
> 更新：每次会话后追加「会话记录」章节。

---

## 一、项目概述

- **产品**：ClassBoard 课程表 Web 应用（自托管，适配电脑与手机浏览器）
- **定位**：单用户家庭使用；课程数据以 Excel 导入为主、手动微调为辅；支持课程提醒、考试与作业记录、数据导出备份
- **技术栈**：后端 Node.js + Express + better-sqlite3（SQLite WAL）；前端 Vue 3 + Vite；单容器 Docker 部署，前端构建产物由后端静态托管，同源 `/api`
- **文档**：`docs/01-PRD`、`02-技术设计`、`03-设计语言`、`04-UI设计`、`05-UI组件参考.html`

---

## 二、会话记录

### 会话 1 · 项目启动与需求文档（早期阶段）

- **用户请求**：创建课程表应用（从零起步）
- **完成内容**：
  - 撰写 `docs/01-PRD-产品需求文档.md`（产品定位、功能模块、交互逻辑、字段约束、验收标准）
  - 生成 6 种视觉风格预览（`examples/style-preview/`：01 清新浅色极简 / 02 科技深色 / 03 纸感笔记 / 04 活泼多彩 / 05 商务专业 / 06 玻璃拟态）
  - 撰写 `docs/02-技术设计文档.md`（系统架构、模块设计、REST 接口契约、数据库设计、Docker 部署设计）

### 会话 2 · 视觉选型与 UI 规范

- **用户请求**：选择视觉风格
- **决策**：选定「清新浅色极简」（风格预览 01）
- **完成内容**：
  - `docs/03-设计语言文档.md` v2.0 定稿（颜色/字体/间距/圆角/阴影/断点 Token、无障碍、图标规范；明确本期不做暗色模式）
  - `docs/04-UI设计文档.md`（信息架构、页面布局线框、响应式规则、交互与三态设计）
  - `docs/05-UI组件参考.html`（可交互组件规范参考页）

### 会话 3 · 前端整体开发（早期 UI 细调，历史会话大量请求）

- **用户请求**：周/日课表页面、课程块、导入向导、PDF 解析等前端开发（含大量 UI 细调）
- **完成内容**：
  - Vue 3 + Vite 工程搭建（`frontend/`），路由周视图/今天/事项/设置
  - 周课表网格（`WeekGrid`、`CourseBlock`、`WeekNav`、`SidePanel`）、日视图（`DayView`）、底部导航/顶栏（`TopBar`、`BottomTabBar`）
  - PDF 课表解析：mupdf (WASM) 提取文本 → 表格重建（`utils/pdf.ts`，51 项单测全绿）
  - PDF 导入向导（`ImportWizard.vue`：上传→解析→预览→覆盖/追加）
  - mock 数据模式（`data/mock.ts`）与课程颜色轮询（`utils/course.ts`）
  - 设计 Token 落地（`styles/tokens.css`）

### 会话 4 · 后端服务实现

- **用户请求**：「下一步要做什么」→ 经 AskUserQuestion 选定「后端服务实现（推荐）」
- **完成内容**：
  - Express + better-sqlite3 后端（`server/`）：学期/节次/课程/考试/作业/导入/日程/访问口令/设置/备份路由
  - scrypt 口令哈希 + 内存会话表；周序号算法 `calcWeekNumber`、单双周过滤 `isCourseVisible`
  - 前端 store 双模式（remote API 优先 + mock 兜底），8 个集成测试全绿

### 会话 5 · 学期开始时间设置

- **用户请求**：「在设置加上设置学期开始时间的功能」
- **完成内容**：设置页学期列表新增「编辑」按钮，行内展开开始/结束日期表单；`store.updateSemester`、`api.updateSemester`

### 会话 6 · 周导航显示第几周

- **用户请求**：「div 这里加一部分显示第几周」
- **完成内容**：周标题区域加「第 N 周」badge（`WeekNav.vue`）

### 会话 7 · 周标题左右偏移修复

- **用户请求**：「div 因为日期不同 所以切换周的时候 这一行会左右偏移」
- **完成内容**：周标题绝对居中（`position:absolute; left:50%; transform:translateX(-50%)`）

### 会话 8 · 日期与周数固定宽度

- **用户请求**：「div 能让左侧的日期固定大小吗 现在这样居中的话 右侧的周数和单双周指示还是会微动」
- **完成内容**：日期 `.wn-dates` min-width 9.5em、周数 `.wn-week` min-width 5.4em，均居中，彻底消除微动

### 会话 9 · PDF 导入课程类型修正

- **用户请求**：「使用pdf导入的一般不是实验课 我们实验课都是手动排的 pdf只是告诉你这个课有实验课 而不是这节课是实验课」
- **完成内容**：PDF 导入课程一律视为理论课（`type:'course'`），实验学时进备注；实验课由用户手动录入

### 会话 10 · 导入上课地点丢失修复

- **用户请求**：「怎么没有导入上课地点」「还是没有地点」
- **完成内容**（两处根因）：
  - 键值字段扫描范围 `parts.slice(2)` → `slice(1)`（真实 PDF 中场地在 parts[1]）
  - `clusterCells` 拼接按行分组 + 行内按 x 排序，修复「场」「地:C3…」跨 span 乱序
  - 新增回归测试，PDF 导入 11 门课全部带地点

### 会话 11 · 手动录入课程

- **用户请求**：「下一步要做什么」→ 经 AskUserQuestion 选定「手动录入课程（推荐）」
- **完成内容**：
  - 导入下拉「手动录入」完整弹窗（`CourseEditor.vue`）：课程名/类型/教师/地点/星期/节次/周次规则/备注
  - `store.addCourse`（remote/mock 双模式）、`api.createCourse`
  - 端到端实测通过（录入实验课 → DB 正确 → 周课表显示 lab 标签+地点）

### 会话 12 · fieldset 奇怪线框

- **用户请求**：「fieldset 为什么有奇怪的线框」
- **完成内容**：`.field` 重置 `border:none; margin:0; padding:0` + legend 缩进重置，浏览器实测边框消失

### 会话 13 · 「每节课调整」标签页

- **用户请求**：「这里再加一个标签页 每节课调整 因为这个页面只能手动导入每周有重复的课程 要是每周上课时间都不一样就无法导入了」
- **完成内容**：
  - `CourseEditor.vue` 双标签页「固定课表 / 每节课调整」，课程名/类型/教师/备注共享
  - 每节课独立设置星期、起始/结束节次、地点、周次；保存为同名课程多条记录（颜色自动复用，后端零改动）

### 会话 14 · 下拉统一为文档要求

- **用户请求**：「全局检查一下下拉表 改成文档要求的」
- **完成内容**：按文档 4.9「一律使用自研 AppSelect，不用原生 select」，替换 CourseEditor 中 6 处原生 `<select>` 为 `AppSelect size="md"`

### 会话 15 · 下拉显示不完全/重叠

- **用户请求**：「下拉栏显示不完全 和元素重叠」
- **完成内容**：`.session-grid` 4 列→2 列（4 列时每列仅 ~107px 触发器溢出）；AppSelect 触发器加 `min-width:0` + `text-overflow:ellipsis`

### 会话 16 · 下拉点击外部关闭修复

- **用户请求**：「这个下拉栏我点击之后不选择 不能点击其他区域关闭 只能选择之后自动收起」
- **完成内容**：原全屏遮罩在弹窗 Teleport/模态层内接不住点击；改为文档级 `mousedown` 监听，点击组件外即关闭；删除 backdrop 及样式

### 会话 17 · 每节课周次规则简化

- **用户请求**：「既然是每节课单独调整了 这个每周 单周 双周是不是多余了 只留自定义的周次选择就可以了」
- **完成内容**：删除「每周/单周/双周/自定义」seg，直接显示周次 chips（1–16 周）；保存一律 `weekType:'custom'` + 勾选周次；`SessionRow` 移除 `weekType`

### 会话 18 · 停止服务

- **用户请求**：「停止服务 明天继续」
- **完成内容**：停止前端（5173）与后端（3000）服务

### 会话 19 · 保存对话数据

- **用户请求**：「把对话数据保存在项目文件夹里」
- **完成内容**：创建本文件 `docs/开发日志.md`，覆盖全部对话记录（含本次扩展）

### 会话 20 · 导入对话数据 + 真实进度盘点与文档更新（2026-08-14，ZCode 会话）

- **用户请求**：「从 Trae 导入对话数据」「读取代码查看真实开发进度 更新所有相关文档」
- **完成内容**：
  - 确认对话数据来源：本文件（会话 19 保存）；Trae 本地库（`ModularData/ai-agent/database.db`）为加密存储无法直接读取，但会话 1-19 记录无缺失
  - 核对真实进度：后端 8 个集成测试全绿、前端 51 项单测全绿（16 week + 8 course + 27 pdf），代码与文档差异盘点如下
  - 发现本文件保存（20:41）之后 20:41–23:06 间 Trae 内仍有开发，日志未覆盖：
    - SettingsView 学期行「切换」按钮 + 「当前」chip（走 `PUT /api/semesters/current`）、节次时间模板只读面板
    - WeekNav「未开学」（weekNumber=0）/「假期」（null）badge
    - ImportWizard 4 步流程完整落地（上传→解析预览→目标学期+追加/覆盖确认→完成），预览含 10×7 网格与行级错误拦截
    - TopBar 提醒铃铛下拉（未读徽标，数据仍为 mock）、桌面「周课表/今天」分段控件（等宽 104px 防位移）
    - WeekGrid 冲突课程错位显示（`computeOverlapGroups`，id 大者右移半格 + 黄色冲突点）、今天列高亮（含「今天」pill，仅本周生效）
    - MattersView 三 Tab（考试空态占位 / 实验课列表 / 作业本地勾选）、DayView「今日作业」区块
    - CourseModal 移动端底部 sheet 上滑动画
    - 后端 schedule 聚合细节：无当前学期时 courses/exams/homework 均返回空数组；course 对象额外含 weekType/weekList 字段
  - 更新全部文档：README、PRD（PDF 导入课程类型判定同步）、技术设计文档（真实实现标注）、设计语言文档（z-index-float 与布局 token）、UI 设计文档（页面清单/导航/未实现项标注）、本日志

### 会话 21 · UI 组件参考页补全（2026-08-14，ZCode 会话）

- **用户请求**：「先审查所有开发文档和 ui设计文档 看看有没有遗漏的地方 然后补全 特别是示例html 这是设计标准」
- **审查结论**：`docs/05-UI组件参考.html`（设计标准）创建于设计早期（8-13 02:38），嵌入的是设计语言 v2.0 契约；与 v2.1/v2.2 及实际 tokens.css 对照发现 4 类问题——token 遗漏（层级 float、布局扩展 token、语义层角色、动效未展示）、规范不一致（时间列宽 64→84px、float 90→80、Modal 440→400px、移动弹层 88vh→85vh、Toast 底部→顶部/3s/fast、周网格 8 节→12 节）、组件缺失（WeekNav、三子 Tab、信息面板、底部 Tab 栏、冲突错位、实验课标签、ErrorState）、结构问题（原生 select 示意违反 4.9、aria 属性不符、空态文案/按钮不符、版本徽标 v2.0）
- **完成内容**（05 组件参考 → v2.2）：
  - 契约块同步 v2.1：补 `--z-index-float: 80`、布局扩展 token（topbar-h / side-panel-w / ph-* / tc-w: 84px）、`--font-family-sans` 归位契约；页面辅助块删除重复定义
  - Token 总览补两个展示组：「语义层角色」（brand/text/bg/border/feedback 21 项）、「动效 / 层级 / 布局」（9 + 4 + 4 项）
  - 新增演示：WeekNav 周导航条（含「假期」徽标形态）、事项页三子 Tab（104px 等宽 + 列表项含完成划线）、信息面板 SidePanel（296px 摘要卡 + 待交作业日期胶囊 warning 三件套）、移动端底部 Tab 栏（4 项）、错误态 ErrorState、图标按钮激活态（brand-subtle 底 + brand 图标 + 未读角标）
  - 周视图网格 8 节 → 12 节（含三节晚自习 19:00-22:30）；数据加冲突课程「科技英语（选修）」（右移半格 + warning 圆点）与实验课「模拟集成电路设计实验」（紫色「实验」标签）
  - 修正规范：原生 select 示意 → 课程色板色卡 + 禁用说明；Select 触发器 `role="combobox"` + aria-label；Toast 顶部居中 + 3s + fast 动效 + 层级 100；Modal 宽 min(400px)、移动端 85vh；空态改为「还没有课程，先导入或手动添加」+ PDF 导入/手动添加双按钮
  - 浏览器实测：三子 Tab 切换、Toast 弹出、课程详情弹窗交互全部通过
- **其他文档修正**：03 设计语言 radius-full 表格 999px→9999px（与 §10 输出一致）、`motion-easing-spring` 标注为与 standard 同值且组件统一引用 standard

### 会话 22 · UI 完善第一批（2026-08-14，ZCode 会话）

- **用户请求**：「记录一下现在项目的状态 便于回退 然后开始完善ui 争取这一次对话把所有需要的ui先做出来 之后我们再进行调整 要遵守相关开发文档 和ui设计文档」
- **完成内容**：
  - 回退点：初始化 git 仓库，基线提交 `6248fb8`（85 文件）；后续每个里程碑独立 commit
  - 全局基础设施：Toast（4.2：顶部居中/四类语义色/3s/fast/层级 100）与确认对话框（4.3：danger 色危险操作）组件
  - `api/client.ts` 补齐全部接口（课程 PUT/DELETE、考试/作业 CRUD+done、节次 CRUD、学期 DELETE、备份导出/恢复、口令 verify/logout/enable/disable）；store 扩展 exams/homework/settings/accessRequired 状态与全部 action
  - 事项页（3.5）：考试 Tab 真实列表（橙色标签、时间倒序、增删改）、实验 Tab（新增实验课预选 lab 类型）、作业 Tab（蓝色标签、勾选完成持久化 PATCH done、状态筛选、增删改）
  - 设置页（3.6）：学期新建（含每周起始日 AppSelect）/删除（确认+自动切换）/切换/行内编辑、节次模板增删（time 输入）、单双周过滤开关、提醒设置（课程/实验/作业三组 + 通知授权提示）、备份导出/导入（覆盖确认）、访问口令开关
  - 口令页（3.7）：AccessView 全屏居中卡片，App 层 `accessRequired` 拦截，校验通过后重新 bootstrap
  - 课程（3.3）：CourseModal 详情加「编辑/删除」（删除二次确认）、CourseEditor 编辑模式（加载课程数据整体更新）+ 课程颜色 8 色色卡选择 + 「保存并继续」（保留星期/地点/颜色预填）
  - 真实数据接入：SidePanel 待交作业、DayView 今日作业（按当天课程过滤）、TopBar 铃铛（今日课程 + 未完成作业，课程/作业两类徽标）
  - 提醒引擎（3.3.2）：`utils/reminder.ts` 每 30s 检查今日课程（普通/实验分流，提前 N 分钟）与作业截止（提前 N 天），Notification API 触发 + 去重
  - 打印/导出 PNG（4.7）：周视图操作条「打印」（@media print A4 横向，仅网格+导航条）与「导出 PNG」（DOM→SVG→canvas，2x 高清）
  - PWA（4.8）：manifest.webmanifest（192/512 PNG 图标，PIL 生成）+ index.html 注册
  - 验证：vue-tsc 类型检查通过、前端 51 单测全绿、`npm run build` 成功、后端 API 全流程 e2e 通过（课程增删改/考试/作业勾选/备份/口令开启-校验-关闭）
- **提交**：`0545cda`（第一批）
- **遗留**：空白格快捷新增课程、CourseModal 考试/作业 Tab、桌面事项页右侧详情面板、骨架屏、Docker 部署（见下方待办）

### 会话 23 · 细节修复与节次模板/时间选择改版（2026-08-14，ZCode 会话）

- **用户请求**：依次提出——不同课程颜色变一样、打印/导出按钮位置与课表一屏显示、PDF 导入预览合并与样式、步骤条多余序号、节次模板区域难看、时间选择器位置不对且与 UI 不一致
- **完成内容**：
  - **颜色修复**：根因 = PDF 导入路径未分配颜色（服务端缺省 course-1），仅手动录入走 8 色轮询；`importCourses` 提交前逐行 `pickCourseColor`（同名复用 + 轮询）；`scripts/reassign-course-colors.mjs` 一次性修复现有数据（同学期同名复用），11 门课恢复 8 色
  - **周视图布局**：打印/导出 PNG 移到课表底部操作条（卡片下方，打印时隐藏）；桌面端节次行高按视口动态计算（`(innerHeight − 244) / 12`，上限 64px 下限 36px，resize 跟随）——1280×720（39px）与 1920×1080（64px）均一屏完整显示
  - **PDF 导入预览**：跨节次课程合并为单个块（`gridRow` 跨行 + **显式 `gridColumn`**——曾因缺列定位被 auto-placement 塞进节次列导致整表错位）；课程块加外框与内边距（浅底+描边，与周课表一致）；修复 `<ol>` 步骤条未重置 `list-style` 导致的多余原生序号
  - **节次模板改版**（方案 3 用户确认）：竖排双列卡片（12 节 → 左列 1-6 / 右列 7-12），悬停显示编辑/删除；卡片行内编辑时间；**时间冲突校验**（保存时校验与其他节次区间重叠，相邻首尾相接允许，冲突卡片内红字提示）；新建节次卡片同校验；<560px 单列
  - **TimePicker 组件**（`components/common/TimePicker.vue` + 5 项单测）：自研时间选择，不用原生 time/datetime-local（原生选择器弹出位置不可控、样式不可定制）；触发器 + 小时/分钟双列滚动面板（5 分钟步进）、sm 32px / md 40px、选中项自动滚到列中央、文档级 mousedown 外部关闭；**全项目时间选择统一替换**——设置页节次模板 4 处、考试编辑弹窗（拆为「日期 + TimePicker」）、作业编辑弹窗（同）
  - **文档同步**：04-UI设计文档 v2.4 新增「4.10 时间选择（TimePicker）控件」（含冲突校验约定），3.6 节次模板实现状态更新；05-UI组件参考新增 TimePicker 演示（CSS/HTML/JS + 可交互）
  - **验证**：56 单测全绿（51 + TimePicker 5）、vue-tsc 通过、构建成功
- **提交**：`abd117d`（颜色）、`de4089f`（周视图布局）、`3452326`/`c4a6c21`/`0cf58d8`（导入预览）、`7d64087`（步骤条）、`3ab91bb`（节次模板）、后续 TimePicker 提交

### 会话 24 · DatePicker 与顶栏事项入口（2026-08-14，ZCode 会话）

- **用户请求**：日期选择也像时间选择一样自研并全局替换；事项页入口在哪（桌面缺失）
- **完成内容**：
  - **DatePicker 组件**（`components/common/DatePicker.vue` + 5 项单测）：触发器 + 日历面板（月份导航/星期表头/日期网格，周一起始），今天 brand 加粗、选中日 brand 底白字、min/max 置灰；Teleport 到 body + fixed 跟随触发器（与 TimePicker 同架构，规避遮挡）
  - **全局替换 6 处原生 date**：设置页学期新建/编辑起止 4 处、考试/作业编辑弹窗各 1 处（「日期 + TimePicker」两段式）；grep 确认原生 date 清零
  - **桌面顶栏事项入口**：UI 文档 1.1 要求桌面顶栏入口但缺失；补铃铛与设置之间的「事项」图标按钮（激活态高亮），图标统一为列表图标（与底部 Tab 一致）
  - **TimePicker 面板遮挡修复**：z-index 方案在部分布局失效 → 改为 Teleport 到 body + fixed 定位跟随触发器（滚动/缩放跟随），彻底规避层叠/裁剪遮挡
  - 文档：04-UI设计 v2.5 新增「4.11 日期选择（DatePicker）控件」；05 组件参考新增 DatePicker 演示
  - 验证：62 单测全绿、vue-tsc 通过
- **提交**：`170cda9`（UI 待办四项）、`59ae8c9`（侧栏组合卡）、`264fd09`（文案修正）、`417317b`/`fb27375`/`25a5049`/`946139e`/`926349b`/`a5df53b`（对齐、冲突分栏、文档同步）

### 会话 26 · 日视图今日课程 bug 修复与侧栏信息卡片合并（2026-08-18，ZCode 会话）

- **用户反馈**：切周后「今天」页显示的不是今天的课（显示的是当前展示周的课），侧栏「本周摘要」的「门课程」语义与统计口径不匹配
- **完成内容**：
  - **日视图今日课程 bug 修复**：store 新增 `todayContext` / `todayWeekNumber` / `todayCourses` / `todayCoursesByWeekday` / `refreshToday()`，按今天真实日期所在周单独拉取数据；DayView.vue 改用 `todayCoursesByWeekday[wd]` 渲染；SidePanel.vue 的「今日课程」计数也改用新源。确保「今天」页始终反映当天真实课程，与周视图切换解耦 `[已实现]`
  - **信息面板卡片合并**：待交作业 + 实践与其他 → 单张组合卡，卡内顶部 seg 分段控件（样式同顶栏视图切换），两段等宽均分；待交作业段 tab 带数量角标（非激活态品牌蓝底白字，激活态品牌蓝底）；空态提示「暂无待交作业，好好休息」 `[已实现]`
  - **文案修正**：本周摘要「门课程」→「节课」（统计周课程块数，非去重课程名） `[已实现]`
  - **侧栏 sticky top 修复**：`sticky top: 84px` → `margin-top: spacing-md`（12px），使初始位置与周导航按钮行上边缘对齐（之前 72px top 未生效因为 margin-top 是解决 sticky 在 grid item 下滚动失效的方案） `[已实现]`
- **提交**：`46b2704`/`59ae8c9`/`264fd09`/`039541b`

### 会话 27 · 课程重叠视图从错位改为 1/3⇄2/3 动态 flex 分栏（2026-08-18，ZCode 会话）

- **用户需求**：左右并排各占一半太窄，想要更美观的重叠课程显示方案
- **完成内容**：
  - **冲突课程分组**：按 isOverlap() 求连通分量，每组一个 `.overlap` flex 容器，列内 `grid-template-columns: 1fr` 单列，容器内通过 `flex-grow` 分配宽度 `[已实现]`
  - **交互规范**：初始均分（1:1），hover 展开（2:1），移开保留最后停留的课程为 2/3；折叠态（1/3）仅显示课程名（字号 --font-size-xs），展开态（2/3）显示名称+地点 `[已实现]`
  - **动画机制**：使用 JS rAF + easeOutCubic 补间驱动（200ms），不依赖 CSS transition（避免 IAB 渲染器冻结 CSS 动画问题） `[已实现]`
  - **修复隐式列问题**：空白槽按钮显式设置 `gridColumn: '1'`，防止 grid 自动放置算法创建隐式列导致列宽被拆分 `[已实现]`
  - **时间列覆盖**：时间列添加 `grid-template-columns: 1fr` 恢复单列，不受 .col 默认规则影响 `[已实现]`
  - **Vue prop 语义修复**：布尔 prop 未传时默认 false 而非 undefined，改 expanded → collapsed 语义避免 solo 课程误折叠 `[已实现]`
  - **文档更新**：04-UI设计文档 v2.7（4.5 节重写为「课程色块冲突错位显示（1/3⇄2/3 动态分栏）」）、开发日志追加会话 26+27 记录
- **示例对比**：examples/overlap-course-options.html（方案对比页，浏览器 localhost:8898 查看）
- **提交**：`fb27375`/`946139e`/`25a5049`/`fd4570a`/`a5df53b`

### 会话 28 · 实验课标签（lab-tag）修复硬编码违规 + 随课程色板（2026-08-18，ZCode 会话）

- **用户反馈**：实验课角标 `font-size: 10px` + `border-radius: 4px` 为硬编码值，不在设计 token 体系内（token 最小字号 11px、圆角 8/10/12/14/9999px），违反 05-UI 组件参考的设计标准；且固定紫色（`--course-3-text`）与卡片主题色脱钩——若实验课本身是紫色会无法区分
- **完成内容**：
  - **CourseBlock.vue `.lab-tag` 样式修复**：`font-size: 10px` → `var(--font-size-xs)`（11px）；`border-radius: 4px` → `var(--radius-full)`（全胶囊）；`color/border` 从固定 `--course-3-text/--course-3-line` 改为动态 `var(--ctext)/var(--cline)`（跟随卡片自身色板）；`padding/line-height` 对齐组件参考（`padding: 0 6px; line-height: 1.6`）`[已实现]`
  - **lab + conflict 并存处理**：冲突圆点从右上角移到左上角（`.course.lab.conflict .conflict-dot { left: 4px; right: auto }`），避免两者重叠 `[已实现]`
  - **文档同步**：04-UI 设计文档 §3.1 D 区更新实验课标签描述（颜色从固定紫改为 --ctext/--cline 动态）、05-UI 组件参考 .course-block__lab 示例更新为 token 化版本、04-UI 设计文档 v2.7 header 追加 v2.8 说明 `[已更新]`
  - 用户选择「随课程色板」方案：标签颜色跟随卡片自身 `--ctext/--cline`（青色系实验课→青边青字标签），背景保持白色以维持视觉层次感
- **提交**：（待本次 commit）

### 会话 25 · UI 待办四项落地（2026-08-15，ZCode 会话）

- **用户请求**：下一轮开发（多选）——空白格快捷新增、课程详情考试/作业 Tab、骨架屏、事项页桌面双栏
- **完成内容**：
  - **空白格快捷新增课程**（PRD 5.2 / UI 3.1）：WeekGrid 每节渲染可点击空白槽（hover 浅底、focus 描边、aria-label 提示），点击打开课程编辑并预填星期/起始节次（`presetWeekday`/`presetPeriod`，结束节次默认 +1）
  - **课程详情考试/作业 Tab**（PRD 5.7）：CourseModal 加考试/作业双 Tab（数量徽标），列出该课程关联记录（橙/蓝标签、完成划线），空态提示
  - **骨架屏**（UI 4.4）：通用 Skeleton 组件三形态（grid 周网格 12×7 / card 列表卡片 / line 详情行），接入周视图（remote 已连且周数据未就绪）与事项页（考试/作业加载中，store.mattersLoading）
  - **事项页桌面双栏**（UI 3.5）：≥1280px 左侧列表 + 右侧 sticky 详情面板（考试/作业/实验课三类详情卡 + 编辑/删除按钮，选中项高亮，编辑保存后刷新选中态）；<1280px 保持原有交互
  - 文档：04-UI设计 v2.6（4 处实现状态标注 + 页面清单）
  - 验证：62 单测全绿、vue-tsc 通过
- **待办剩余**：Docker 部署（技术设计第 7 章）

---

## 三、当前代码结构

```
d:\ClassBoard
├── docs/                    需求/技术/设计语言/UI/组件参考 + 本日志
├── examples/style-preview/  6 种风格预览（保留参考）
├── frontend/                Vue 3 + Vite
│   ├── public/              favicon.svg / icons.svg / sample.pdf
│   ├── src/
│   │   ├── api/client.ts    REST 客户端（context/schedule/semesters/courses/import/settings）
│   │   ├── components/      common(AppSelect) / course(CourseEditor) / import(ImportWizard) /
│   │   │                    layout(TopBar, BottomTabBar) / schedule(WeekGrid, CourseBlock,
│   │   │                    WeekNav, SidePanel, CourseModal)
│   │   ├── router/          周/日/事项/设置 四视图 + 文档标题
│   │   ├── stores/schedule.ts  唯一 store（双模式 remote/mock；weekExams/weekHomework 管道已备）
│   │   ├── styles/          tokens.css（Token）/ style.css（reset 与全局动效）
│   │   ├── utils/           pdf.ts / course.ts / week.ts（含单测）
│   │   ├── data/            mock.ts（动态学期/11 门课/作业/实践）/ pdf-fixture.json
│   │   └── views/           WeekView / DayView / MattersView / SettingsView
│   └── scripts/             mupdf-test / extract-pdf / extract-words / cmap-test（开发辅助）
└── server/                  Express + better-sqlite3
    ├── lib/                 db / validate / access / week / settings / errors
    ├── routes/              semesters / periods / courses / exams / homework / import / schedule(+context) / access / settings / backup
    └── test/api.test.js     8 个集成测试
```

## 四、待办任务（2026-08-14 会话 22 更新后状态）

**已在本批次完成**（会话 22）：事项页考试/作业真实数据与增删改、设置页全功能（学期/节次/提醒/备份/口令）、前端口令页、课程编辑/删除入口、课程颜色手动修改、保存并继续、SidePanel/DayView/铃铛真实数据、提醒引擎、打印/导出 PNG、PWA。

**剩余待办**：

- [x] ~~Docker 部署~~（2026-08-19 会话 31 已部署至 NAS 192.168.10.110:3005）
- [ ] 生产环境反向代理 + HTTPS（可选，技术设计 7.4）

**测试基线**：后端 8 集成测试（`cd server && npm test`）、前端 62 单测（`cd frontend && npm test`）全绿。

## 五、继续开发指引

- 前端 dev：`cd frontend && npm run dev`（5173）
- 后端 dev：`cd server && npm run dev`（3000）
- 构建：`cd frontend && npm run build`
- 后端集成测试：`cd server && npm test`
- 前端单测：`cd frontend && npm test`
- 数据库：`server/lib/data/classboard.db`（SQLite WAL）

## 六、关键约定

- 下拉选择一律用自研 `AppSelect`，不使用原生 `<select>`（文档 4.9；外部关闭为文档级 mousedown 监听，无遮罩）
- PDF 导入课程一律视为理论课；实验课手动录入
- 周序号算法 `calcWeekNumber`、单双周过滤 `isCourseVisible`（`server/lib/week.js`）
- 课程冲突显示：同日同节次重叠时渲染为 flex 分栏容器（`.overlap`），初始均分（1:1），hover 展开（2:1），最后停留保留展开；折叠态仅显示课程名，展开态显示名称+地点；动画由 JS rAF 补间驱动（`computeOverlapGroups` + `isOverlap` 连通分量分组）；不做冲突拦截
- 无当前学期时 `/api/schedule` 的 courses/exams/homework 均返回空数组（weekNumber=null）
- 视觉基准：清新浅色极简（设计语言文档 v2.0），本期无暗色模式

### 会话 29 · 长按拖拽失败定位与修复（2026-08-18）

- 用户反馈课程长按拖拽始终提示「调整失败，请重试」。
- 通过浏览器真实长按拖拽复现，确认失败发生在 API 请求之前，实际错误为 `calcWeekNumber is not defined`。
- 原因：`WeekGrid.vue` 使用 `calcWeekNumber` 与 `parseDate` 计算学期总周数，但遗漏了 `@/utils/week` 导入。
- 修复：补充 `import { calcWeekNumber, parseDate } from '@/utils/week'`；同时保留分步错误提示，后续可区分目标无效、保留其他周失败和创建本周副本失败。
- 验证：`vue-tsc --noEmit` 通过，前端 62 项测试通过，后端 `/api/context` 返回 200；服务端课程更新/创建接口实测正常。

### 会话 30 · 审计遗留中等项修复（2026-08-19，ZCode 会话）

- **用户请求**：处理审计遗留的中等项
- **完成内容**：
  - **weekInfoOf 展示周错误**：remote 模式下原实现始终返回当前展示周的周号（切周后漂移）；改为仅当日期属于当前展示周时使用服务端周号，其余日期按本地学期起止计算 `[已修复]`
  - **remote 模式颜色分配基准**：`courses.value` 在 remote 模式原为 mock 数据，新增课程颜色轮询基准错误；bootstrap 与 setSemester 增加 `loadCourses()` 拉取当前学期课程全量（`api.listCourses`），导入/手动录入颜色分配与真实数据对齐 `[已修复]`
  - **TopBar 导入下拉外部关闭**：原只有提醒菜单有遮罩关闭，导入菜单无外部点击关闭；补文档级 mousedown 监听（与 AppSelect 外部关闭同约定），点击菜单外关闭 `[已修复]`
  - **404 路由兜底**：未知路径重定向到 `/week`（默认首页）；浏览器实测 `http://localhost:5173/nonexistent-page` 自动跳转周课表 `[已修复]`
  - **maxWeeks 边界**：CourseEditor/WeekGrid 学期总周数按服务端 `MAX_WEEKS=30` 截断，避免前端可选周次超过服务端校验上限 `[已修复]`
  - **MOCK_PRACTICE 类型化**：SidePanel 实践条目补 `PracticeItem` 接口类型（无固定时间语义） `[已修复]`
  - **conflictGroups 缓存**：冲突连通分量分组由模板内函数调用改为 computed 缓存，仅在课程数据变化时重算 `[已修复]`
  - **exportPng Firefox 兼容**：SVG foreignObject 对链接样式表解析不可靠（Firefox 易导出空白）；改为导出前将每个元素计算样式内联到 style 属性 `[已修复]`
  - 未处理项：移动端滑动提示、空学期引导（功能空缺需产品决策，不在本次范围）
- **验证**：vue-tsc 通过、前端 62 单测全绿、后端 8 集成测试全绿、浏览器实测 404 跳转与导入下拉外部关闭
- **提交**：`5687c0a`

### 会话 31 · Docker 部署至 NAS（2026-08-19，ZCode 会话）

- **用户请求**：在本地网络 NAS 192.168.10.110 的 /vol1/1000/ClassBoard 尝试 Docker 部署
- **完成内容**：
  - **部署文件**：新增 `Dockerfile`（node:22-alpine 多阶段构建，非 root 运行，HEALTHCHECK）、`docker-compose.yml`（端口/时区/数据卷可配置）、`.dockerignore`（构建上下文裁剪）
  - **NAS 环境确认**：Debian 12（x86_64）类 NAS，Docker 29.6.1 + Compose v5.3.1，`sour` 用户在 docker 组；`/vol1/1000/ClassBoard` 可写
  - **构建问题排查**：① better-sqlite3@13 要求 Node≥22 → 基础镜像 node:20-alpine 升级为 node:22-alpine；② node-gyp 下载 headers 超时 → 构建阶段走宿主机 clash 代理（`build.network: host` + `HTTP(S)_PROXY=http://127.0.0.1:7890`）；③ Dockerfile 漏 `COPY server/` 导致运行时找不到 index.js → 补齐；④ WeekGrid 未使用变量 `col` 导致 vue-tsc 构建失败 → 改为 `(_, idx)` 已提交
  - **端口**：3000/3001 已被 NAS 其他服务占用，使用 `CLASSBOARD_PORT=3005` 启动（技术设计 7.3 预留的端口可配）
  - **数据持久化**：`CLASSBOARD_DATA_DIR=/app/data` + bind 挂载 `/vol1/1000/ClassBoard/data`，classboard.db 已落盘（WAL 模式）
  - **访问地址**：http://192.168.10.110:3005（首页 200，/api/context 正常返回）
- **提交**：`1767d00`（Dockerfile/docker-compose/.dockerignore/nas_ssh.py/WeekGrid 未用变量修复）

### 会话 32 · NAS 部署后问题修复（2026-08-19，ZCode 会话）

- **用户反馈**：设置页点日期选择器「下个月」时下拉栏收起点不到；NAS 学期名称乱码（用户手动删除）
- **完成内容**：
  - **DatePicker/TimePicker 面板误关修复**：Teleport 到 body 的面板不在 `root` 内，`onDocMouseDown` 只检查 `root.contains` 导致面板内点击被误判为外部点击；改为同时检查面板 ref（`panel.value.contains`），面板内月份切换/时间选择不再误关，外部点击仍正常关闭 `[已修复]`
  - **SettingsView aria-label 插值修复**：`aria-label="编辑第 {{ p.index }} 节"` 是静态字符串不插值，按钮标签显示字面量 `{{ p.index }}`；改为 `:aria-label="\`编辑第 ${p.index} 节\`"` `[已修复]`
  - **NAS 学期重建**：用 Python UTF-8 重新创建「2026-2027 学年第一学期」（08-17 至 12-13），节次模板 12 节完整，无乱码
- **验证**：vue-tsc 通过、62 单测全绿；浏览器实测：点「下个月」面板保持打开且月份切换正常、外部点击正常关闭、编辑/删除按钮标签正确插值
- **提交**：`4c87506`

### 会话 33 · iPhone 15 移动端适配（2026-08-19，ZCode 会话）

- **用户请求**：iPhone 15 屏幕规格（393×852，3x）进行 UI 适配；手机顶栏显示不全
- **完成内容**：
  - **顶栏移动端适配**：学期切换限宽 110px（省略号）；导入课程按钮移动端仅图标（34px 方钮，文字隐藏）；≤360px 窄屏（iPhone SE 1 代）学期选择进一步收窄至 76px `[已实现]`
  - **实测验证**（浏览器 393×852 / 375 / 320 三档视口）：顶栏全部元素完整显示、无横向溢出；周课表 1064px 横向滚动（可视 346px）；今天/事项/设置页均无横向溢出；底部导航 53px 含安全区 `[已验证]`
  - 既有适配确认：viewport 含 `viewport-fit=cover`、底部栏 `env(safe-area-inset-bottom)`、断点体系 768/1280 合理，无需改动
- **提交**：`e7192c6`

### 会话 34 · 移动端周课表双日视图（2026-08-19，ZCode 会话）

- **用户请求**：移动端周课表改为双日视图——默认显示今天+明天（周日只显示当天），左右滑动逐日切换（左滑→更晚一天，右滑→更早一天），跨周数据正确
- **用户决策**：移动端禁用长按拖拽（避免与滑动冲突）；隐藏周导航（改为双日标题条 + 今天按钮）
- **完成内容**：
  - **store.coursesOfDate(date)**：按日期独立过滤课程（`weekInfoOf` 算该日周号 + `isVisibleInWeek` + weekday 匹配），跨周（周六+周日、滑入下周）天然正确；remote 模式 `courses` 全量已由 loadCourses 拉取，不依赖单周 weekContext `[已实现]`
  - **WeekGrid days prop**：双日视图渲染 `mobileColumns`（逐日过滤 + 冲突标注复用 computeOverlapGroups）；表头日期/星期/今天高亮按实际日期计算；移动端 CSS 改 `repeat(2, minmax(0,1fr))` 去掉 1064px 横向滚动，`touch-action: pan-y` 让水平滑动留给应用 `[已实现]`
  - **滑动切日**：pointer 事件（覆盖触摸与鼠标），水平位移 >50px 且 |dx|>|dy| 判定，window 级 pointerup 收尾（pointer capture/中断也能正确触发）；垂直滚动不干扰 `[已实现]`
  - **WeekView**：`dayOffset`（相对今天天数偏移）+ 双日标题条「周三 8/19 · 周四 8/20」+ 今天按钮；周日只显示当天；移动端隐藏 WeekNav `[已实现]`
  - **拖拽**：双日视图下 `onCoursePointerDown` 直接 return（禁用），桌面 7 列不受影响 `[已实现]`
- **验证**：vue-tsc 通过、62 单测全绿；浏览器 393×852 实测默认「周三 8/19 · 周四 8/20」双列、周导航隐藏；1280px 桌面 7 列 + 周导航无回归；NAS 已部署（滑动切日需真机触摸验证，IAB 无法模拟完整 pointer 序列）
- **提交**：`8ad235d`

### 会话 35 · 双日视图滑动切日动画（2026-08-19，ZCode 会话）

- **用户反馈**：左右滑动切日像闪现刷新，需要优雅动画
- **完成内容**：
  - **WeekView 滑动动画**：`slideDir` 记录滑动方向，`<Transition mode="out-in">` 包裹 WeekGrid（`:key="dayOffset"` 触发切换）；左滑（next）新内容从右滑入、旧内容向左滑出，右滑（prev）反向；位移 24px + 透明度，`--motion-duration-slow`（220ms）+ `--motion-easing-standard` `[已实现]`
  - **今天按钮**：非今天时按方向动画归零（offset>0 视为 next，<0 视为 prev） `[已实现]`
- **验证**：vue-tsc 通过、62 单测全绿；NAS 已部署（动画效果需真机滑动确认）
- **提交**：`9ecc601`

### 会话 36 · 双日视图推入式滑动动画（2026-08-19，ZCode 会话）

- **用户反馈**：滑动动画仍像闪现，要页面从左右滑入的感觉
- **完成内容**：
  - **推入式滑动**：`<Transition>` 默认 mode（新旧同时动画），enter 元素 `position:absolute; inset:0; z-index:1` 覆盖在旧页上方从侧边 100% 滑入，旧页原位向反方向 100% 滑出——视觉连续无空白、无闪烁 `[已实现]`
  - **方向**：左滑（next）新页从右滑入、旧页向左滑出；右滑（prev）反向 `[已实现]`
  - **兼容性修复**：`mode="sync"` 是 Vue 3.6+ 特性，项目 Vue 3.5 类型不支持导致 Docker 构建失败（本地 node_modules 版本差异未暴露）；改用默认 mode + 绝对定位覆盖实现同效果 `[已修复]`
- **验证**：vue-tsc 通过、62 单测全绿；NAS 构建产物确认含 `position:absolute;inset:0` 覆盖式动画；效果需真机滑动确认
- **提交**：`5080d7f`

### 会话 37 · 双日视图滑动动画 bug 修复（2026-08-19，ZCode 会话）

- **用户反馈**：滑动有概率触发 UI 闪现；除底栏外其他 UI 只在左半边完整显示、有缩放
- **根因**：
  - **布局错乱**：enter 元素 `position:absolute; inset:0` 的定位包含块是最近的 positioned 祖先——`.sched-scroll` 无 `position:relative`，enter 元素相对视口定位（宽 393px）而正常网格在容器内（346px），导致错位/缩放感
  - **闪现**：快速连续滑动时多个 Transition 动画叠加，enter 元素残留绝对定位
- **修复**：
  - `.sched-scroll` 加 `position: relative`（enter 元素以此为定位包含块）+ enter 加 `width: 100%` `[已修复]`
  - **动画锁**：`sliding` 标志，切换动画进行中（260ms）忽略新滑动，避免连续滑动动画叠加 `[已修复]`
- **验证**：vue-tsc 通过、62 单测全绿；NAS 构建产物确认 `position:relative` 已生效；效果需真机滑动确认
- **提交**：`e9b033d`

### 会话 38 · 双日标题条美化（2026-08-19，ZCode 会话）

- **用户反馈**：双日标题条按钮离顶栏太近、显示太单调
- **完成内容**：
  - **间距**：标题条上下边距 `margin: var(--spacing-lg) 0`（原仅底部 spacing-md），与顶栏拉开距离 `[已实现]`
  - **标题**：加日历图标（品牌色），标题与图标对齐 `[已实现]`
  - **今天按钮**：改为品牌色实心胶囊按钮（`--color-brand` 底 + 白字 + `--radius-full` + `--shadow-btn`），hover 变 `--color-brand-hover`，与设计 token 体系一致 `[已实现]`
- **验证**：vue-tsc 通过、62 单测全绿；NAS 构建产物确认新样式已生效
- **提交**：`<本次 commit>`
